export default class Particle {

    constructor(x, y, intColor, extColor, mass) {
      
        this.x = x;
        this.y = y;
        this.intColor = intColor;
        this.extColor = extColor;
        this.mass = mass;

    }

    // Add initial velocity to each
    // Given force on particle return new position given 
    // an interval of time

}
